# Maps core journey QA — 2026-07-27

## Baseline and deployment

- Started from `origin/main` at `6f1d43b` (merge of PR #63).
- The merged PR head passed `Production CI` run `30220602647` (run 197). The checked-in workflow still requires the same named job for pushes and pull requests targeting `main`.
- Main protection was verified directly on 2026-07-24 as strict `Production CI`, admin enforcement, conversation resolution, and no force pushes or deletions. The local GitHub CLI credential expired before this run, so the protection endpoint could not be independently reread.
- No newer pre-existing open Maps deploy preview was available through accessible repository metadata. The new PR preview must be checked after publication.

## Interactive coverage

Production: `https://maps.lalgeo.com/render/lalgeosurvey`

| Area | Desktop 1440×900 | Tablet 768×1024 | Mobile 390×844 | Notes |
| --- | --- | --- | --- | --- |
| Shell, menus, toolbar, close controls | Pass | Pass | Pass | No document overflow, clipping, or console warnings/errors. |
| Browser project creation | Pass | Pass | Pass | Browser-only temporary data; no cloud write. |
| Point draw/select/details/table | Pass | Pass | Pass | Point created, selected, autosaved, and opened in Feature Details. |
| Line/polygon drawing | Pass (regression suite) | Pass (regression suite) | Pass (regression suite) | Geometry rendering/selection contracts cover all three types; live point path was repeated manually. |
| Attribute review/edit and table controls | Pass | Pass | Pass | Existing durable editor-label and details journey coverage passed. |
| Search and keyboard paths | Pass | Pass | Pass | Search, shortcut, menu, focus-restoration, and off-canvas tests passed. |
| Import/export and print/PDF preparation | Pass | Pass | Pass | Durable project/layer import and output preparation journeys passed. |
| Basemap, directions, location, cloud entry points | Pass/Skip | Pass/Skip | Pass/Skip | UI and recovery paths passed; location permission and authenticated Dropbox writes skipped to avoid external side effects. |
| Console/runtime health | Pass | Pass | Pass | No errors or warnings at any tested width. |

## Defect and fix

The table reported `0 selected` while leaving **Show selected** enabled. Activating it produced a valid but confusing empty table with no prerequisite or recovery explanation. The symmetric **Show unselected** path could do the same after selecting every row, and the filter buttons exposed only a visual active class rather than toggle state.

Failure evidence: `screenshots/selection-filter-zero-enabled-production-2026-07-27.png`.

The fix treats filters as a complete state machine:

- exposes `aria-pressed` for all three views;
- disables filter choices that are guaranteed to be empty;
- explains every disabled state in visible hover and accessible names;
- disables the already-active all-rows choice;
- normalizes stale selected/unselected modes back to all rows before rendering, including after selection changes or record removal.

## Durable verification

- Added `test:table-selection-filter-states` and made it required by Production CI.
- `npm ci`: pass.
- All 38 Maps test/benchmark scripts: pass.
- `npm run build`: pass.
- `git diff --check`: pass.

## Human inspection

1. Open the Maps deploy preview at 1440×900 and create a browser-only project with one point.
2. Open the table and confirm **Show selected** is disabled at `0 selected`, with the explanation “Select one or more features before showing selected rows.”
3. Select the row and confirm **Show selected** enables; activate it and confirm `aria-pressed=true`.
4. Clear the selection and confirm the table recovers to all rows instead of remaining empty.
5. Select all rows and confirm **Show unselected** is disabled with a concrete recovery explanation.
6. Repeat at 768×1024 and 390×844, checking focus, clipping, overlap, and console/network errors.

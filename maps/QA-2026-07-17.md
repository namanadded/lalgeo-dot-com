# Maps core journey QA — 2026-07-17

Target: `https://maps.lalgeo.com/render/lalgeosurvey` at production commit `3f4433a`.

## Coverage

| Journey | Result | Evidence |
| --- | --- | --- |
| Desktop shell and map render (1440×900) | Pass | Apple map loaded; menus, editing, map, measurement, search, Look Around, and table controls were present without clipping. |
| Tablet shell (768×1024) | Pass | Compact menu and Tools entry appeared; welcome controls and map workspace remained reachable. |
| Mobile shell (390×844) | Pass | Mobile menu sheet opened with New, Import, Workspace, Edit, Map, Share & Export, and Settings & Help groups. |
| Project creation | Pass | Created browser-only project `Nightly QA 2026-07-17` with a point layer; required-field disabled state and helper text behaved correctly. |
| Point creation and selection | Pass | Placed a point, opened Feature Details, exposed read-only attributes, and enabled Undo. Browser-only data was discarded on reload. |
| Empty/disabled states | Pass | New Layer, Share, Export, Print, Undo, and Redo supplied contextual disabled labels before a project or edit existed. |
| Keyboard guidance | Pass | `A` and `Escape` guidance was exposed; menu shortcuts and accessible expanded state were present. |
| Dropbox disconnected recovery | **Fail** | A normal signed-out load logged `Connect your Dropbox account to list files` twice as console errors even though disconnected state is expected. Fixed in this change. |
| Console health | **Fail** | The two expected Dropbox authentication errors were the only observed errors. No unrelated warnings were observed. |
| Latest deploy preview | Skip | No deploy-preview URL was available from repository metadata, and the local Netlify CLI could not be obtained in this environment. |
| Dropbox/cloud mutation | Skip | Intentionally not connected or written; production/cloud data remained untouched. |
| Location permission | Skip | No browser permission was accepted during unattended QA. The location control and label were present. |
| Import/export/print payload completion | Covered by regression suite | Deterministic source checks cover project/layer imports, export/print entry points, workflow hierarchy, map types, toolbar overflow, and route configuration. No production file upload or download was persisted. |
| Line/polygon details and editing | Covered by regression suite | The mobile feature-details and selection-highlight checks verify interactive line/polygon overlays, shared details, selection clearing, and styling. |

## Defect and fix

`refreshDropboxSurveyList` converted the expected HTTP 401 signed-out response into an exception and logged it with `console.error`. The new recovery branch clears stale cloud state, restores the connect UI, records a low-noise workspace debug event, and returns without throwing. Unexpected non-authentication failures still reach `console.error`.

## Automated verification

- All deterministic maps scripts configured in Production CI.
- Newly enabled CI coverage: mobile menu organization, mobile Feature Details, workflow UI, and map-control layout.
- New disconnected-recovery regression ensures HTTP 401 is handled before generic failures, stale cloud state is cleared, the connect entry point returns, and no warning/error is emitted for the expected state.
- `npm ci`
- `npm run test:mapkit-route`
- `npm run build`

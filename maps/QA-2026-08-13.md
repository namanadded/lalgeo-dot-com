# Maps core journey QA — 2026-08-13

## Baseline and environments

- Source: `origin/main` at `5ad7e2b` on `codex/nightly-maps-core-journey-qa-2026-08-13`.
- Production: `https://maps.lalgeo.com/`.
- Latest available pre-existing Maps preview: PR #129 at `https://deploy-preview-129--lalgeo-maps.netlify.app/`.
- Temporary data: browser-only projects named `Nightly QA 2026-08-13` and `Focus QA`; no Dropbox/cloud writes and no location permission.

## Interactive coverage

| Journey | Result | Evidence / notes |
| --- | --- | --- |
| Production desktop shell and onboarding | Pass | Start Project, New Project, project name, default point layer, Create, initial placement, map click, selection, and Feature Details all completed. No production warning/error console entries. |
| Mobile 390×844 selection/details | Fail → fixed | `Properties` opened the Feature Details sheet visually but left `document.activeElement` on `BODY`. Failure screenshot: `screenshots/feature-details-focus-production-2026-08-13.png`. |
| Mobile close/recovery | Fail → fixed | The compact selection inspector is rebuilt while details open, so the original trigger cannot be reused directly. The fix restores the newly rendered Properties/Edit trigger, with Map canvas as the durable fallback. |
| Explicit Edit feature path | Pass after fix | The focus contract sends editing directly to the first writable attribute after mobile sheet exclusivity is established. |
| Mobile clipping/overflow | Pass | 390 px root remained bounded (`html clientWidth = scrollWidth = 390`) with overflow hidden; details sheet and selected-feature action tray stayed within the viewport. |
| Tablet responsive coverage | Automated pass / interactive skip | The browser viewport override remained at 390 px when 768×1024 was requested. Dedicated toolbar, layout, sheet, and responsive regression scripts passed. |
| Latest deploy preview #129 | Pass with known limitation | Full shell/onboarding rendered. MapKit rejected the Netlify preview origin because the token allows `*.lalgeo.com`; interactive map gestures were skipped there. |
| Cloud and precise location | Skip | Avoided real cloud mutations and location permission by design. Signed-out/recovery behavior remains covered by deterministic tests. |

## Fix and regression coverage

- Explicit Feature Details entry now remembers the invoking control/action, focuses Close for read-only details, and focuses the first editable attribute for Edit.
- Close, Escape, and Done share the same restoration path. If the mobile action tray was re-rendered, focus returns to the corresponding new Properties/Edit control; otherwise it returns to the original connected trigger or Map canvas.
- Added `test-feature-details-focus-journey.mjs` and required it in Production CI.
- Updated existing mobile details, selection-inspector, mobile tools, and off-canvas assertions to enforce the expanded contract.

## Verification

- All 83 Maps `test:*` scripts passed.
- `npm run build` passed with Next.js 16.2.9.
- Production CI coverage guard confirms all 83 Maps test scripts are required exactly once.
- Production had no warning/error console entries; preview showed only the expected MapKit origin rejection.

## CI and protection

- The checked-in `Production CI` workflow still runs on pull requests into `main` and pushes to `main`, and the coverage guard passes.
- Direct administrative branch-protection and baseline workflow-run reads were unavailable because the local GitHub CLI credential is expired. Protection was last directly verified on 2026-08-11 as strict required `Production CI`, admin enforcement, stale-review dismissal, conversation resolution, and no force pushes/deletions.

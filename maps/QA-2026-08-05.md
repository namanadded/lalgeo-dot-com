# Maps QA — 2026-08-05

## Scale and cloud reliability

### Change

Direct revision-controlled cloud uploads below the 16 MiB resumable threshold now recover from a lost final response by verifying the provider's new revision, exact byte size, and content hash. The provider-independent helper never blindly repeats an ambiguous write. New-file adds remain unchanged because an autorenamed destination cannot be proven safely without a returned path.

### Synthetic baseline and result

No Dropbox credentials or real files were used. Mocked, non-sensitive payloads represented approximately 1,000, 10,000, and 40,000 features at 1, 8, and 15 MiB.

| Features | Payload | Baseline after committed response loss | Result | Uploads | Metadata verifications |
| ---: | ---: | --- | --- | ---: | ---: |
| 1,000 | 1 MiB | False failure | Recovered | 1 | 1 |
| 10,000 | 8 MiB | False failure | Recovered | 1 | 1 |
| 40,000 | 15 MiB | False failure | Recovered | 1 | 1 |

The broader 1k/10k/50k benchmark covers startup/import, pan/zoom, rendering, serialization/autosave, export, memory, and recovery. On this run, the 50k / 30.44 MiB case measured 48.46 ms startup, 54.93 ms import, 4.62 ms pan/zoom, 0.86 ms rendering, 76.81 ms autosave, 56.14 ms export, 32.10 ms recovery, and 60.91 MiB serialization heap growth. Quota failure preserved the prior stored bytes.

### Compatibility, integrity, and rollback

- Project schema, serialized bytes, cloud paths, OAuth, provider configuration, and stale-revision conflict behavior are unchanged.
- Only revision-controlled Dropbox updates below 16 MiB use recovery verification. Non-retryable auth, quota, and conflict failures still fail immediately.
- A result is accepted only when Dropbox reports a changed revision, exact size, and exact content hash. If verification is unavailable or inconclusive, the original retryable error remains visible and prior local/remote data is not overwritten by a second request.
- Roll back by reverting the single implementation commit; no migration or cloud cleanup is required.

### Exact human verification

Using only an approved disposable Dropbox account:

1. Connect, list, open, save, rename, disconnect, and reconnect a synthetic project smaller than 16 MiB.
2. During a revision-controlled save, let Dropbox commit and then interrupt only the upload response; confirm the UI reports success after one metadata lookup and makes no second upload.
3. Reopen the file and compare feature count, geometry, nested attributes, style, schema, and revision metadata.
4. Repeat with a stale revision and confirm a conflict is shown without treating unrelated current metadata as the saved result.
5. Exercise offline/retry, auth, and quota failures; confirm inconclusive verification retains the error and the current local project plus prior remote revision remain intact.
6. Recheck a new-file add with a lost response; confirm it fails safely rather than guessing an autorenamed destination.

## Core journey QA

### Baseline and deploys

- Source: `origin/main` at `5e87f72379ee20b6324cf367029f482955a59efe` (merge of PR #110).
- Production: `https://maps.lalgeo.com/render/lalgeosurvey`.
- Latest available preview: PR #111 at `https://deploy-preview-111--lalgeo-maps.netlify.app/render/lalgeosurvey`.
- Production CI: run 328 (`30973099912`) completed successfully for the baseline commit in 49 seconds.
- Branch protection: last directly verified on 2026-08-03 as strict required **Production CI**, admin enforcement, stale-review dismissal, conversation resolution, and no force pushes/deletions. The current unauthenticated UI and expired local `gh` credential cannot reread administrative protection settings; the checked-in workflow still targets pull requests into and pushes to `main`.

### Interactive coverage

| Journey | Desktop 1440×900 | Tablet 768×1024 | Mobile 390×844 | Notes |
| --- | --- | --- | --- | --- |
| Production shell, map, toolbar, responsive overflow | Pass | Pass | Pass | No document-width overflow; persistent map and table controls stayed in bounds. |
| Project and first layer creation | Pass | Pass | Pass | Created a temporary browser-only project with a Polygon layer; no cloud or production data write. |
| Add menu and geometry inventory | Pass | Pass | Pass | Point, Line, Polygon, Import Data, Import Photos, and Add Layer entries remained reachable; responsive resize correctly closed the transient menu and reset `aria-expanded`. |
| Location loading and denied-permission recovery | Pass | Pass | Pass | Mobile control exposed a polite error, actionable Try again, and named close control without a console error. |
| Search, directions, map types, Look Around, panels, table, fields/edit/archive, import/export, print/PDF, share, cloud entry points | Pass | Pass | Pass | Exercised directly where safe and cross-checked with the complete deterministic journey suite. Dropbox writes and browser permission acceptance were intentionally skipped. |
| Latest preview shell and responsive overflow | Pass | Pass | Pass | Complete shell loaded. Preview logged only Netlify's known `frame has already mounted` warning; no application error or overflow. |
| Preview MapKit-dependent interaction | Skip | Skip | Skip | The deploy-preview origin is outside the production MapKit token allowlist; shell, status, and responsive contracts remain testable. |
| Voice-navigation geolocation failure recovery | Fail | Fail | Fail | Route voice tracking used blocking alerts for permission denial, unavailable position, and timeout, with no loading, retry, watcher cleanup, or focus-restoration contract. |

### Defect and fix

The one-shot **My Location** and route-acquisition flows already used durable status regions, but **Start Voice Navigation** bypassed that recovery model. A watcher failure interrupted users with a browser alert and left the route sidebar without an actionable state.

The fix adds a complete voice-navigation state machine:

- loading, active, error, and stopped announcements in the route sidebar;
- disabled/busy start and enabled stop states that prevent duplicate watchers;
- plain-language geolocation errors and a focused Try again action;
- empty-route and unsupported-browser recovery;
- watcher and speech cleanup on errors and Stop Voice;
- focus restoration to the invoking Start Voice Navigation button;
- shared behavior for initial and alternate-route render paths.

Failure evidence: `maps/screenshots/voice-navigation-location-recovery-production-2026-08-05.jpg` captures the production location recovery surface used as the expected non-blocking model; the voice-navigation branch did not provide an equivalent surface before this fix.

### Verification

- Optimized Next.js production build: pass.
- All 70 `test:*` Maps scripts: pass.
- Expanded `test:location-journey`: pass, including voice loading/active/error/stopped, retry, busy controls, cleanup, focus restoration, and no legacy alerts.
- `test:directions-journey`: pass.
- `git diff --check`: pass.

### Human inspection

1. Open the deploy preview, search for a destination, and choose **Get Directions**.
2. When a route appears, choose **Start Voice Navigation** and deny location access (or force unavailable/timeout).
3. Confirm the start button becomes busy while waiting, then the sidebar shows **Voice navigation unavailable** with **Try again** focused; no browser alert appears.
4. Allow location and retry. Confirm the sidebar announces **Voice navigation active**, Start is disabled, and Stop Voice is enabled.
5. Choose **Stop Voice**. Confirm tracking/speech stop, the stopped status is announced, and focus returns to Start Voice Navigation.
6. Select an alternate route and repeat to verify the same behavior.

Risk is limited to route voice-navigation controls and geolocation watcher lifecycle. Project formats, feature data, imports/exports, cloud storage, MapKit routing, and production settings are unchanged.
